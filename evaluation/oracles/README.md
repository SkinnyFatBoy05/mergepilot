# Hidden evaluator boundary

These placeholder manifests document the trusted evaluator inputs. Production oracle source is deliberately excluded from the runner image by `deploy/evaluator.Dockerfile`; the recorded suite exposes only SHA-256 oracle identities and bounded pass/fail categories.
